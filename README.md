# Base-L2
Base L2 Starter: Solidity+Hardhat+Frontend. Contract=BaseCounter.sol(inc/dec uint val). Deploy via Hardhat (base,sepolia). Frontend (HTML+JS+ethers v6) connects MetaMask, shows val, calls increment(). Steps: copy files→npm i→deploy→insert addr→open index.html.
